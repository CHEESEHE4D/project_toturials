# 游戏界面整合进度

更新时间：2026-09-12

## 步骤 A：素材清单、主题回退、响应式静态布局

状态：代码完成，自动构建通过。

- 三主题均有实际存在的 540×960 `background.jpg`，路径集中在 `src/config/assets.ts`。
- 可选背景视频未配置时不会发起请求；未来视频失败时保留图片底图。
- 花园花瓣、星空微光为边缘程序动画，不接收点击，暂停时冻结。
- 训练舞台为居中 9:16 区域；顶部、轨道、教学、双指标、摄像头与任务计数已排版。
- 2026-09-12：三主题 manifest / background 均返回 HTTP 200；响应式开发页无控制台错误。
- 2026-09-12：训练背景替换为用户提供的萌宠、花园、星空竖图，统一压缩为 540×960；界面底板与背景遮罩按参考风格增强了清晰度和层次。

## 步骤 B：教学素材接口、统一时间轴、30 任务轨道

状态：代码完成，正式教学素材待提供。

- 新谱面为 `STRAIGHT → HOOK → FIST`，3 秒/任务，10 轮，30 任务，正好 90 秒。
- 3 秒倒数独立于正式时间；轨道位置、目标切换和教学卡均读取同一个高精度训练时钟。
- 背景视频只跟随播放/暂停，不再驱动任务结束；轨道没有独立 CSS 循环动画。
- 三首 91.72 秒主题音乐已分别接入萌宠、花园、星空；倒数结束后与训练时间轴从 0 秒同步播放，暂停/丢手/切后台时冻结，恢复重播当前任务时重定位。
- 当前仅使用彼此不同的原创 SVG 动作示意，明确标注“待专业审核”，未伪装成真实教学素材。

## 步骤 C：真实识别、保持、节奏、暂停与记录

状态：代码与自动验收完成，真实设备验收归入步骤 D。

- MediaPipe 的 STRAIGHT/HOOK/FIST/UNKNOWN 与跟踪状态直接进入训练引擎；没有随机或模拟成绩。
- 正确姿势累计目标 2 秒；错误/UNKNOWN 停止累计但不清空；持续 LOST 自动暂停。
- 节奏到达和保持完成独立结算；任务结束只结算一次。
- 恢复会倒数并重播当前 3 秒窗口，临时到达/保持会被撤销。
- Vitest 23/23 通过：包含累计保持跨 UNKNOWN、错误不累计、一次性结算、丢手恢复及延迟帧下 90 秒/30 任务完整收尾。
- 本地浏览器已验证倒数、轨道移动、暂停冻结、恢复重播；正式入口没有注入模拟识别。

## 步骤 D：真机、性能、正式素材

状态：待人工设备与素材。

- 生产构建已同步到 `docs/app/`；本地 production preview 成功加载三主题卡片，控制台无警告/错误。
- 生产路径下主题 manifest、背景图和 Google Hand Landmarker 模型均返回 HTTP 200。
- 待 iPhone Safari 与 Android Chrome 实测摄像头、音频许可、后台恢复及连续三轮性能。
- 待三套经过专业审核的动作教学素材。
- 待 Perfect、Good、保持完成反馈音效；三主题背景音乐已接入，仍需真机检查移动浏览器首播延迟与音量。
- 钩拳分类阈值本次未放宽，仍需收集真机诊断数据后单独评估。
